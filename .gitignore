# build output
dist/
.astro/

# local verification scratch output
.playwright-cli/

# raw, full-res image-generation output -- prompt.md stays tracked for
# reproducibility, but the source renders are large (~2MB each) and only
# the processed derivative under src/assets/ actually ships
.claude/image-generation/**/*.png
.claude/image-generation/**/*.jpg
.claude/image-generation/**/*.webp

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store
